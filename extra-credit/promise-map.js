import { promises } from "fs";

Promise.map = (collection, iterator) => {


 return Promise.all


};


//Info On promise maps
// Promises use .then to chain new Promises
// //
// var promise1 = new Promise(function(resolve, reject) {
//   setTimeout(function() {
//     resolve('foo')
//   }, 300)
// })
// promise1.then(function(value))
// return promise1
